name: homebrew-store-cdn

services:
  homebrew-store-cdn:
    build: .
    image: fabiocdo/homebrew-store-cdn:latest
    container_name: homebrew-store-cdn
    ports:
      - "8080:80"
    volumes:
      - ${CDN_DATA_DIR:-./data}:/data
    env_file:
      - .env
    restart: unless-stopped
