# Basic server values (minimal required configuration)
SERVER_IP=127.0.0.1/1
SERVER_PORT=80
ENABLE_SSL=false

# TLS certificates live under configs/certs/ (the defaults are mounted into the container)
TLS_CRT=/app/configs/certs/tls.crt
TLS_KEY=/app/configs/certs/tls.key

# Logging level for the watcher/indexer
LOG_LEVEL=info

# Optional overrides (add any of the WATCHER_*/AUTO_INDEXER_* variables if you want custom behavior)
